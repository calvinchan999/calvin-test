name: CI

on:
  push:
    tags:
      - "v*"
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.14.1]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
      - name: Show npm version
        run: npm -v
      - run: npm install
      - run: npm install -g @angular/cli@11.2.19
      - name: Install kendo-angular-layout
        run: npm install --save @progress/kendo-angular-layout
      - name: Activate Kendo UI License
        run: npx kendo-ui-license activate
        env:
          KENDO_UI_LICENSE: ${{ secrets.KENDO_UI_LICENSE }}
      - name: Build Application
        run: npm run build-arcs
      - name: Azure Static Web Apps Deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_DEPLOYMENT_TOKEN  }}
          action: "upload"
          skip_api_build: true
          skip_app_build: true
          app_location: "dist/schmidt-arcs-website"
      - name: Zip File And Create New Directory
        run: tar -zcvf fobo-amr-web.tar.gz dist && mkdir ./dist/zip && mv ./fobo-amr-web.tar.gz ./dist/zip
      - name: Upload to Azure File Container
        uses: lauchacarro/Azure-Storage-Action@v1.1
        with:
          connection-string: ${{ secrets.CONNECTION_STRING }}
          folder: dist/zip
          blob-container-name: fobo-amr-web

          